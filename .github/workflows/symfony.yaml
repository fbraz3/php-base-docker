name: Build Symfony Image

on:
  workflow_call:
    inputs:
      php_version:
        required: true
        type: string
      architecture:
        required: true
        type: string
      is_latest:
        required: true
        type: boolean

jobs:
  build_symfony:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Symfony image
        uses: docker/build-push-action@v6
        with:
          platforms: ${{ inputs.architecture }}
          context: .
          file: flavors/symfony/Dockerfile
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            PHP_VERSION=${{ inputs.php_version }}
          tags: |
            fbraz3/symfony-cli:${{ inputs.php_version }}
            ${{ inputs.is_latest && 'fbraz3/symfony-cli:latest' || '' }}

      - name: Test Symfony with -v to ensure it loads properly
        run: |
          docker run --rm fbraz3/symfony-cli:${{ inputs.php_version }} -v || exit 1

      - name: Push Symfony image
        uses: docker/build-push-action@v6
        with:
          platforms: ${{ inputs.architecture }}
          context: .
          file: flavors/symfony/Dockerfile
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            PHP_VERSION=${{ inputs.php_version }}
          tags: |
            fbraz3/symfony-cli:${{ inputs.php_version }}
            ${{ inputs.is_latest && 'fbraz3/symfony-cli:latest' || '' }}